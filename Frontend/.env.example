# The URL of your deployed backend (no trailing slash, no /api at the end).
#
# Local development:
# REACT_APP_API_URL=http://localhost:5000
#
# Production (Vercel) — point this at your Render backend URL:
# REACT_APP_API_URL=https://your-backend-name.onrender.com
REACT_APP_API_URL=http://localhost:5000
